interface:
  display_name: "Kizashi"
  short_description: "Turn signals into testable hypotheses"
  default_prompt: "Use $kizashi to initialize a Kizashi workspace, then run signal, hypo/hypothesize, review, or positioning from configured sources."
